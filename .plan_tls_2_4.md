# TLS 指纹 ②④ 实施计划

> 前置：①（模板维度化 os/client_type）与③（实时刷新）已完成并验证。本计划只覆盖 ② 采集器分平台 + ④ 账号 OS×client 矩阵绑定。
> 决策已确认：路由判维度 + 账号矩阵解析 / 非 OpenAI 用账号默认 OS 绑定 / 降级兼容旧单值（无数据迁移）。

---

## 问题④：账号按 OS(+client) 绑定不同模板

### 4.1 数据结构（账号 Extra，新增键，不动旧键）
- 新增 `tls_fingerprint_bindings`：`map[string]int64`，key = `os` 或 `os/client_type`（如 `windows`、`macos/codex-cli`），value = profileID（`-1`=同维度随机，`0`/缺省=回退）。
- 旧键 `tls_fingerprint_profile_id`（单值）、`tls_fingerprint_router_id` 全部保留。
- **解析优先级**（出站时）：
  1. router 判定出维度 `dim`（os 或 os/client）→ 查 `bindings[os/client]` → 查 `bindings[os]`
  2. 命中 → 该 profileID；未命中或无 bindings → **降级**用旧单值 `tls_fingerprint_profile_id`
  3. 仍无 → 内置默认。
- 旧的「router 规则里直接带 profile_id」作为 dim 解析失败时的兜底保留。

### 4.2 后端改动
- `internal/service/account.go`：
  - 新增 `GetTLSFingerprintBindings() map[string]int64`（容错解析 json.Number/float64）。
  - 新增 `GetTLSFingerprintProfileIDForDimension(os, clientType string) int64`：按上面优先级返回，含旧单值降级。
- `internal/service/tls_fingerprint_profile_service.go`：
  - 新增 `getRandomProfileForDimension(platform, os, clientType)`（复用现有随机逻辑，加 os/client 过滤）。
  - 新增 `ResolveTLSProfileForDimension(account, os, clientType)`：账号矩阵解析入口。
- `internal/service/openai_tls_fingerprint_router.go`（核心接线）：
  - router 的 `MatchRequest` 现返回 `ProfileID`。**扩展** match 结果，让 router 规则能输出「判定维度」`os`/`client_type`（见 4.4 router 规则扩展）。
  - `resolveOpenAITLSFingerprintRuntime`：拿到 router 判定的 `(os, client)` → 调 `account.GetTLSFingerprintProfileIDForDimension` → 有则覆盖 runtime.Profile；维持现有 UA/Originator 覆盖逻辑不变。
  - 若 router 未配维度或未命中：保持现状（用 router 规则自带 profileID，再降级账号单值）——**完全向后兼容现有 109 账号**。
- 非 OpenAI 平台（Anthropic/Kiro/Grok）：`ResolveTLSProfile(account)` 增加「读账号默认 OS」分支——账号 Extra 加 `tls_fingerprint_default_os`，有则走 `ResolveTLSProfileForDimension(account, defaultOS, "")`，无则保持旧单值逻辑。

### 4.3 DTO + 前端账号编辑
- `dto/types.go` + `mappers.go`：AccountDTO 增 `TLSFingerprintBindings map[string]int64`、`TLSFingerprintDefaultOS string`，读写 Extra。
- `EditAccountModal.vue` / `CreateAccountModal.vue`：
  - OpenAI 区块：在 router 选择器下方，新增「**OS×客户端 绑定矩阵**」小表。行=已配置维度，每行：维度 key（os 或 os/client）+ profile 下拉（default/random/具体，按 platform+os+client 过滤）。可增删行。
  - Anthropic/Kiro/Grok 区块：新增「默认 OS」下拉（windows/macos/linux）+ 该 OS 的 profile 下拉（即单维度 binding）。
  - `tlsFingerprintProfileOptions.ts`：过滤函数从「按 platform」升级为「按 platform + os + client_type」，新增维度参数。
- `BulkEditAccountModal.vue`：批量设置时，矩阵作为可选整体字段写入。

### 4.4 Router 规则扩展（让 router 输出判定维度）
- `model/tls_fingerprint_router.go`：`TLSFingerprintRouterRule` 增 `OS string`、`ClientType string`（规则命中后输出的判定维度）。旧规则这两字段空 = 行为不变。
- `tls_fingerprint_router_service.go`：`MatchRequest` 结果结构体增 `OS`/`ClientType`，命中后带出。
- `TLSFingerprintRoutersModal.vue`：规则编辑表单每行增「判定OS」「判定客户端」两个可选输入。
- DB：`tls_fingerprint_routers.rules` 是 JSONB，**无需迁移**（结构体加字段即可）。

---

## 问题②：采集器分平台 + 采集样本带维度

### 5.1 采集样本加 os 维度
- `ent/schema/tls_fingerprint_capture_sample.go`：加 `field.String("os").Default("")`（client_type 已存在）。
- `ent/schema/tls_fingerprint_capture_session.go` / `..._event.go`：同步加 `os`（采集链路传递）。
- 迁移 `187_add_tls_fingerprint_capture_os.sql`：3 张表加 `os VARCHAR(20) DEFAULT ''`（纯追加）。
- `go generate ./ent` 重新生成。
- 采集解析：从 User-Agent 推断 os（已有 UA 解析点），或从采集引导 URL 的 hint 参数 `?os=macos&client=codex-cli` 读取，写入样本。
- 导入到 profile：`tls_fingerprint_profile_import.go` 把样本的 `os`/`client_type` 映射进新建 profile。
- 采集样本表格（ProfilesModal capture tab）：增 os/client_type 两列展示。

### 5.2 采集器页面按平台拆分
- 现状：`TLSFingerprintCollectorView.vue` 一页吃所有平台，platform 是自由输入框。
- 改造（最小侵入，不新建路由文件，复用现有页 + query）：
  - 页面顶部 platform 从「自由输入」改为「**平台选择 tab**」(openai / anthropic / grok)，切换后下方采集指令区**只渲染该平台真实存在的 客户端×OS 组合**。
  - 每个平台一个「指令清单」数据结构（前端常量）：
    - **OpenAI**：Codex CLI(win/mac/linux) / Codex exec / ChatGPT Desktop / Node SDK / Python SDK / curl
    - **Anthropic**：Claude Code(win/mac/linux) / claude print / Node SDK / Python SDK / curl
    - **Grok**：grok 官方客户端 / curl
  - 每条采集指令在 capture URL 上自动带 `?os=...&client=...` hint，落到样本维度（配合 5.1）。
  - 现有「How it works / 支持的 transport / 成功行为」等通用区块保留，仅指令区按平台切换。

---

## 实施顺序（增量、每步可验证、不破坏现有）

1. **④-后端基础**：account.go 维度解析 + service 维度 resolve + 单测（不接线，纯新增方法）。
2. **④-router 扩展**：rule 加 os/client 字段 + MatchRequest 带出 + 接线 resolveOpenAITLSFingerprintRuntime + 单测。
3. **④-DTO/前端**：AccountDTO 字段 + 账号编辑矩阵 UI + options 过滤升级 + i18n。
4. **②-采集维度**：capture schema 加 os + 迁移 + ent 生成 + 导入映射 + 样本表格列。
5. **②-采集器分平台**：CollectorView 平台 tab + 指令清单 + hint 参数。
6. 全量 `go build ./...` / `go vet` / 后端 TLS 测试 / 前端 typecheck+lint+test。

## 兼容性保证
- 所有新字段默认空；现有 109 个账号（router_id=2、单值 profile_id）走降级路径，行为零变化。
- 无数据迁移；DB 改动全是 `ADD COLUMN ... DEFAULT ''` 追加。
- router 旧规则、旧出站路径、HTTP/1.1 出站全部不变。

## 不在本次范围（避免过度设计）
- HTTP/2 出站指纹定制（你之前勾选纳入，但风险最高，建议作为本计划完成后的独立 P4，单独验证再做）。如你坚持本次做，我会在第 6 步后追加。
- client_version 维度（粒度只到 os×client_type）。
