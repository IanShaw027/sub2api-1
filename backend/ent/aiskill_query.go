// Code generated by ent, DO NOT EDIT.

package ent

import (
	"context"
	"database/sql/driver"
	"fmt"
	"math"

	"entgo.io/ent"
	"entgo.io/ent/dialect"
	"entgo.io/ent/dialect/sql"
	"entgo.io/ent/dialect/sql/sqlgraph"
	"entgo.io/ent/schema/field"
	"github.com/Wei-Shaw/sub2api/ent/aiskill"
	"github.com/Wei-Shaw/sub2api/ent/aiskilllike"
	"github.com/Wei-Shaw/sub2api/ent/aiskillreview"
	"github.com/Wei-Shaw/sub2api/ent/aiskillrun"
	"github.com/Wei-Shaw/sub2api/ent/aiskillsettlement"
	"github.com/Wei-Shaw/sub2api/ent/aiskillversion"
	"github.com/Wei-Shaw/sub2api/ent/predicate"
)

// AISkillQuery is the builder for querying AISkill entities.
type AISkillQuery struct {
	config
	ctx             *QueryContext
	order           []aiskill.OrderOption
	inters          []Interceptor
	predicates      []predicate.AISkill
	withVersions    *AISkillVersionQuery
	withRuns        *AISkillRunQuery
	withReviews     *AISkillReviewQuery
	withLikes       *AISkillLikeQuery
	withSettlements *AISkillSettlementQuery
	modifiers       []func(*sql.Selector)
	// intermediate query (i.e. traversal path).
	sql  *sql.Selector
	path func(context.Context) (*sql.Selector, error)
}

// Where adds a new predicate for the AISkillQuery builder.
func (_q *AISkillQuery) Where(ps ...predicate.AISkill) *AISkillQuery {
	_q.predicates = append(_q.predicates, ps...)
	return _q
}

// Limit the number of records to be returned by this query.
func (_q *AISkillQuery) Limit(limit int) *AISkillQuery {
	_q.ctx.Limit = &limit
	return _q
}

// Offset to start from.
func (_q *AISkillQuery) Offset(offset int) *AISkillQuery {
	_q.ctx.Offset = &offset
	return _q
}

// Unique configures the query builder to filter duplicate records on query.
// By default, unique is set to true, and can be disabled using this method.
func (_q *AISkillQuery) Unique(unique bool) *AISkillQuery {
	_q.ctx.Unique = &unique
	return _q
}

// Order specifies how the records should be ordered.
func (_q *AISkillQuery) Order(o ...aiskill.OrderOption) *AISkillQuery {
	_q.order = append(_q.order, o...)
	return _q
}

// QueryVersions chains the current query on the "versions" edge.
func (_q *AISkillQuery) QueryVersions() *AISkillVersionQuery {
	query := (&AISkillVersionClient{config: _q.config}).Query()
	query.path = func(ctx context.Context) (fromU *sql.Selector, err error) {
		if err := _q.prepareQuery(ctx); err != nil {
			return nil, err
		}
		selector := _q.sqlQuery(ctx)
		if err := selector.Err(); err != nil {
			return nil, err
		}
		step := sqlgraph.NewStep(
			sqlgraph.From(aiskill.Table, aiskill.FieldID, selector),
			sqlgraph.To(aiskillversion.Table, aiskillversion.FieldID),
			sqlgraph.Edge(sqlgraph.O2M, false, aiskill.VersionsTable, aiskill.VersionsColumn),
		)
		fromU = sqlgraph.SetNeighbors(_q.driver.Dialect(), step)
		return fromU, nil
	}
	return query
}

// QueryRuns chains the current query on the "runs" edge.
func (_q *AISkillQuery) QueryRuns() *AISkillRunQuery {
	query := (&AISkillRunClient{config: _q.config}).Query()
	query.path = func(ctx context.Context) (fromU *sql.Selector, err error) {
		if err := _q.prepareQuery(ctx); err != nil {
			return nil, err
		}
		selector := _q.sqlQuery(ctx)
		if err := selector.Err(); err != nil {
			return nil, err
		}
		step := sqlgraph.NewStep(
			sqlgraph.From(aiskill.Table, aiskill.FieldID, selector),
			sqlgraph.To(aiskillrun.Table, aiskillrun.FieldID),
			sqlgraph.Edge(sqlgraph.O2M, false, aiskill.RunsTable, aiskill.RunsColumn),
		)
		fromU = sqlgraph.SetNeighbors(_q.driver.Dialect(), step)
		return fromU, nil
	}
	return query
}

// QueryReviews chains the current query on the "reviews" edge.
func (_q *AISkillQuery) QueryReviews() *AISkillReviewQuery {
	query := (&AISkillReviewClient{config: _q.config}).Query()
	query.path = func(ctx context.Context) (fromU *sql.Selector, err error) {
		if err := _q.prepareQuery(ctx); err != nil {
			return nil, err
		}
		selector := _q.sqlQuery(ctx)
		if err := selector.Err(); err != nil {
			return nil, err
		}
		step := sqlgraph.NewStep(
			sqlgraph.From(aiskill.Table, aiskill.FieldID, selector),
			sqlgraph.To(aiskillreview.Table, aiskillreview.FieldID),
			sqlgraph.Edge(sqlgraph.O2M, false, aiskill.ReviewsTable, aiskill.ReviewsColumn),
		)
		fromU = sqlgraph.SetNeighbors(_q.driver.Dialect(), step)
		return fromU, nil
	}
	return query
}

// QueryLikes chains the current query on the "likes" edge.
func (_q *AISkillQuery) QueryLikes() *AISkillLikeQuery {
	query := (&AISkillLikeClient{config: _q.config}).Query()
	query.path = func(ctx context.Context) (fromU *sql.Selector, err error) {
		if err := _q.prepareQuery(ctx); err != nil {
			return nil, err
		}
		selector := _q.sqlQuery(ctx)
		if err := selector.Err(); err != nil {
			return nil, err
		}
		step := sqlgraph.NewStep(
			sqlgraph.From(aiskill.Table, aiskill.FieldID, selector),
			sqlgraph.To(aiskilllike.Table, aiskilllike.FieldID),
			sqlgraph.Edge(sqlgraph.O2M, false, aiskill.LikesTable, aiskill.LikesColumn),
		)
		fromU = sqlgraph.SetNeighbors(_q.driver.Dialect(), step)
		return fromU, nil
	}
	return query
}

// QuerySettlements chains the current query on the "settlements" edge.
func (_q *AISkillQuery) QuerySettlements() *AISkillSettlementQuery {
	query := (&AISkillSettlementClient{config: _q.config}).Query()
	query.path = func(ctx context.Context) (fromU *sql.Selector, err error) {
		if err := _q.prepareQuery(ctx); err != nil {
			return nil, err
		}
		selector := _q.sqlQuery(ctx)
		if err := selector.Err(); err != nil {
			return nil, err
		}
		step := sqlgraph.NewStep(
			sqlgraph.From(aiskill.Table, aiskill.FieldID, selector),
			sqlgraph.To(aiskillsettlement.Table, aiskillsettlement.FieldID),
			sqlgraph.Edge(sqlgraph.O2M, false, aiskill.SettlementsTable, aiskill.SettlementsColumn),
		)
		fromU = sqlgraph.SetNeighbors(_q.driver.Dialect(), step)
		return fromU, nil
	}
	return query
}

// First returns the first AISkill entity from the query.
// Returns a *NotFoundError when no AISkill was found.
func (_q *AISkillQuery) First(ctx context.Context) (*AISkill, error) {
	nodes, err := _q.Limit(1).All(setContextOp(ctx, _q.ctx, ent.OpQueryFirst))
	if err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nil, &NotFoundError{aiskill.Label}
	}
	return nodes[0], nil
}

// FirstX is like First, but panics if an error occurs.
func (_q *AISkillQuery) FirstX(ctx context.Context) *AISkill {
	node, err := _q.First(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return node
}

// FirstID returns the first AISkill ID from the query.
// Returns a *NotFoundError when no AISkill ID was found.
func (_q *AISkillQuery) FirstID(ctx context.Context) (id int64, err error) {
	var ids []int64
	if ids, err = _q.Limit(1).IDs(setContextOp(ctx, _q.ctx, ent.OpQueryFirstID)); err != nil {
		return
	}
	if len(ids) == 0 {
		err = &NotFoundError{aiskill.Label}
		return
	}
	return ids[0], nil
}

// FirstIDX is like FirstID, but panics if an error occurs.
func (_q *AISkillQuery) FirstIDX(ctx context.Context) int64 {
	id, err := _q.FirstID(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return id
}

// Only returns a single AISkill entity found by the query, ensuring it only returns one.
// Returns a *NotSingularError when more than one AISkill entity is found.
// Returns a *NotFoundError when no AISkill entities are found.
func (_q *AISkillQuery) Only(ctx context.Context) (*AISkill, error) {
	nodes, err := _q.Limit(2).All(setContextOp(ctx, _q.ctx, ent.OpQueryOnly))
	if err != nil {
		return nil, err
	}
	switch len(nodes) {
	case 1:
		return nodes[0], nil
	case 0:
		return nil, &NotFoundError{aiskill.Label}
	default:
		return nil, &NotSingularError{aiskill.Label}
	}
}

// OnlyX is like Only, but panics if an error occurs.
func (_q *AISkillQuery) OnlyX(ctx context.Context) *AISkill {
	node, err := _q.Only(ctx)
	if err != nil {
		panic(err)
	}
	return node
}

// OnlyID is like Only, but returns the only AISkill ID in the query.
// Returns a *NotSingularError when more than one AISkill ID is found.
// Returns a *NotFoundError when no entities are found.
func (_q *AISkillQuery) OnlyID(ctx context.Context) (id int64, err error) {
	var ids []int64
	if ids, err = _q.Limit(2).IDs(setContextOp(ctx, _q.ctx, ent.OpQueryOnlyID)); err != nil {
		return
	}
	switch len(ids) {
	case 1:
		id = ids[0]
	case 0:
		err = &NotFoundError{aiskill.Label}
	default:
		err = &NotSingularError{aiskill.Label}
	}
	return
}

// OnlyIDX is like OnlyID, but panics if an error occurs.
func (_q *AISkillQuery) OnlyIDX(ctx context.Context) int64 {
	id, err := _q.OnlyID(ctx)
	if err != nil {
		panic(err)
	}
	return id
}

// All executes the query and returns a list of AISkills.
func (_q *AISkillQuery) All(ctx context.Context) ([]*AISkill, error) {
	ctx = setContextOp(ctx, _q.ctx, ent.OpQueryAll)
	if err := _q.prepareQuery(ctx); err != nil {
		return nil, err
	}
	qr := querierAll[[]*AISkill, *AISkillQuery]()
	return withInterceptors[[]*AISkill](ctx, _q, qr, _q.inters)
}

// AllX is like All, but panics if an error occurs.
func (_q *AISkillQuery) AllX(ctx context.Context) []*AISkill {
	nodes, err := _q.All(ctx)
	if err != nil {
		panic(err)
	}
	return nodes
}

// IDs executes the query and returns a list of AISkill IDs.
func (_q *AISkillQuery) IDs(ctx context.Context) (ids []int64, err error) {
	if _q.ctx.Unique == nil && _q.path != nil {
		_q.Unique(true)
	}
	ctx = setContextOp(ctx, _q.ctx, ent.OpQueryIDs)
	if err = _q.Select(aiskill.FieldID).Scan(ctx, &ids); err != nil {
		return nil, err
	}
	return ids, nil
}

// IDsX is like IDs, but panics if an error occurs.
func (_q *AISkillQuery) IDsX(ctx context.Context) []int64 {
	ids, err := _q.IDs(ctx)
	if err != nil {
		panic(err)
	}
	return ids
}

// Count returns the count of the given query.
func (_q *AISkillQuery) Count(ctx context.Context) (int, error) {
	ctx = setContextOp(ctx, _q.ctx, ent.OpQueryCount)
	if err := _q.prepareQuery(ctx); err != nil {
		return 0, err
	}
	return withInterceptors[int](ctx, _q, querierCount[*AISkillQuery](), _q.inters)
}

// CountX is like Count, but panics if an error occurs.
func (_q *AISkillQuery) CountX(ctx context.Context) int {
	count, err := _q.Count(ctx)
	if err != nil {
		panic(err)
	}
	return count
}

// Exist returns true if the query has elements in the graph.
func (_q *AISkillQuery) Exist(ctx context.Context) (bool, error) {
	ctx = setContextOp(ctx, _q.ctx, ent.OpQueryExist)
	switch _, err := _q.FirstID(ctx); {
	case IsNotFound(err):
		return false, nil
	case err != nil:
		return false, fmt.Errorf("ent: check existence: %w", err)
	default:
		return true, nil
	}
}

// ExistX is like Exist, but panics if an error occurs.
func (_q *AISkillQuery) ExistX(ctx context.Context) bool {
	exist, err := _q.Exist(ctx)
	if err != nil {
		panic(err)
	}
	return exist
}

// Clone returns a duplicate of the AISkillQuery builder, including all associated steps. It can be
// used to prepare common query builders and use them differently after the clone is made.
func (_q *AISkillQuery) Clone() *AISkillQuery {
	if _q == nil {
		return nil
	}
	return &AISkillQuery{
		config:          _q.config,
		ctx:             _q.ctx.Clone(),
		order:           append([]aiskill.OrderOption{}, _q.order...),
		inters:          append([]Interceptor{}, _q.inters...),
		predicates:      append([]predicate.AISkill{}, _q.predicates...),
		withVersions:    _q.withVersions.Clone(),
		withRuns:        _q.withRuns.Clone(),
		withReviews:     _q.withReviews.Clone(),
		withLikes:       _q.withLikes.Clone(),
		withSettlements: _q.withSettlements.Clone(),
		// clone intermediate query.
		sql:  _q.sql.Clone(),
		path: _q.path,
	}
}

// WithVersions tells the query-builder to eager-load the nodes that are connected to
// the "versions" edge. The optional arguments are used to configure the query builder of the edge.
func (_q *AISkillQuery) WithVersions(opts ...func(*AISkillVersionQuery)) *AISkillQuery {
	query := (&AISkillVersionClient{config: _q.config}).Query()
	for _, opt := range opts {
		opt(query)
	}
	_q.withVersions = query
	return _q
}

// WithRuns tells the query-builder to eager-load the nodes that are connected to
// the "runs" edge. The optional arguments are used to configure the query builder of the edge.
func (_q *AISkillQuery) WithRuns(opts ...func(*AISkillRunQuery)) *AISkillQuery {
	query := (&AISkillRunClient{config: _q.config}).Query()
	for _, opt := range opts {
		opt(query)
	}
	_q.withRuns = query
	return _q
}

// WithReviews tells the query-builder to eager-load the nodes that are connected to
// the "reviews" edge. The optional arguments are used to configure the query builder of the edge.
func (_q *AISkillQuery) WithReviews(opts ...func(*AISkillReviewQuery)) *AISkillQuery {
	query := (&AISkillReviewClient{config: _q.config}).Query()
	for _, opt := range opts {
		opt(query)
	}
	_q.withReviews = query
	return _q
}

// WithLikes tells the query-builder to eager-load the nodes that are connected to
// the "likes" edge. The optional arguments are used to configure the query builder of the edge.
func (_q *AISkillQuery) WithLikes(opts ...func(*AISkillLikeQuery)) *AISkillQuery {
	query := (&AISkillLikeClient{config: _q.config}).Query()
	for _, opt := range opts {
		opt(query)
	}
	_q.withLikes = query
	return _q
}

// WithSettlements tells the query-builder to eager-load the nodes that are connected to
// the "settlements" edge. The optional arguments are used to configure the query builder of the edge.
func (_q *AISkillQuery) WithSettlements(opts ...func(*AISkillSettlementQuery)) *AISkillQuery {
	query := (&AISkillSettlementClient{config: _q.config}).Query()
	for _, opt := range opts {
		opt(query)
	}
	_q.withSettlements = query
	return _q
}

// GroupBy is used to group vertices by one or more fields/columns.
// It is often used with aggregate functions, like: count, max, mean, min, sum.
//
// Example:
//
//	var v []struct {
//		CreatedAt time.Time `json:"created_at,omitempty"`
//		Count int `json:"count,omitempty"`
//	}
//
//	client.AISkill.Query().
//		GroupBy(aiskill.FieldCreatedAt).
//		Aggregate(ent.Count()).
//		Scan(ctx, &v)
func (_q *AISkillQuery) GroupBy(field string, fields ...string) *AISkillGroupBy {
	_q.ctx.Fields = append([]string{field}, fields...)
	grbuild := &AISkillGroupBy{build: _q}
	grbuild.flds = &_q.ctx.Fields
	grbuild.label = aiskill.Label
	grbuild.scan = grbuild.Scan
	return grbuild
}

// Select allows the selection one or more fields/columns for the given query,
// instead of selecting all fields in the entity.
//
// Example:
//
//	var v []struct {
//		CreatedAt time.Time `json:"created_at,omitempty"`
//	}
//
//	client.AISkill.Query().
//		Select(aiskill.FieldCreatedAt).
//		Scan(ctx, &v)
func (_q *AISkillQuery) Select(fields ...string) *AISkillSelect {
	_q.ctx.Fields = append(_q.ctx.Fields, fields...)
	sbuild := &AISkillSelect{AISkillQuery: _q}
	sbuild.label = aiskill.Label
	sbuild.flds, sbuild.scan = &_q.ctx.Fields, sbuild.Scan
	return sbuild
}

// Aggregate returns a AISkillSelect configured with the given aggregations.
func (_q *AISkillQuery) Aggregate(fns ...AggregateFunc) *AISkillSelect {
	return _q.Select().Aggregate(fns...)
}

func (_q *AISkillQuery) prepareQuery(ctx context.Context) error {
	for _, inter := range _q.inters {
		if inter == nil {
			return fmt.Errorf("ent: uninitialized interceptor (forgotten import ent/runtime?)")
		}
		if trv, ok := inter.(Traverser); ok {
			if err := trv.Traverse(ctx, _q); err != nil {
				return err
			}
		}
	}
	for _, f := range _q.ctx.Fields {
		if !aiskill.ValidColumn(f) {
			return &ValidationError{Name: f, err: fmt.Errorf("ent: invalid field %q for query", f)}
		}
	}
	if _q.path != nil {
		prev, err := _q.path(ctx)
		if err != nil {
			return err
		}
		_q.sql = prev
	}
	return nil
}

func (_q *AISkillQuery) sqlAll(ctx context.Context, hooks ...queryHook) ([]*AISkill, error) {
	var (
		nodes       = []*AISkill{}
		_spec       = _q.querySpec()
		loadedTypes = [5]bool{
			_q.withVersions != nil,
			_q.withRuns != nil,
			_q.withReviews != nil,
			_q.withLikes != nil,
			_q.withSettlements != nil,
		}
	)
	_spec.ScanValues = func(columns []string) ([]any, error) {
		return (*AISkill).scanValues(nil, columns)
	}
	_spec.Assign = func(columns []string, values []any) error {
		node := &AISkill{config: _q.config}
		nodes = append(nodes, node)
		node.Edges.loadedTypes = loadedTypes
		return node.assignValues(columns, values)
	}
	if len(_q.modifiers) > 0 {
		_spec.Modifiers = _q.modifiers
	}
	for i := range hooks {
		hooks[i](ctx, _spec)
	}
	if err := sqlgraph.QueryNodes(ctx, _q.driver, _spec); err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nodes, nil
	}
	if query := _q.withVersions; query != nil {
		if err := _q.loadVersions(ctx, query, nodes,
			func(n *AISkill) { n.Edges.Versions = []*AISkillVersion{} },
			func(n *AISkill, e *AISkillVersion) { n.Edges.Versions = append(n.Edges.Versions, e) }); err != nil {
			return nil, err
		}
	}
	if query := _q.withRuns; query != nil {
		if err := _q.loadRuns(ctx, query, nodes,
			func(n *AISkill) { n.Edges.Runs = []*AISkillRun{} },
			func(n *AISkill, e *AISkillRun) { n.Edges.Runs = append(n.Edges.Runs, e) }); err != nil {
			return nil, err
		}
	}
	if query := _q.withReviews; query != nil {
		if err := _q.loadReviews(ctx, query, nodes,
			func(n *AISkill) { n.Edges.Reviews = []*AISkillReview{} },
			func(n *AISkill, e *AISkillReview) { n.Edges.Reviews = append(n.Edges.Reviews, e) }); err != nil {
			return nil, err
		}
	}
	if query := _q.withLikes; query != nil {
		if err := _q.loadLikes(ctx, query, nodes,
			func(n *AISkill) { n.Edges.Likes = []*AISkillLike{} },
			func(n *AISkill, e *AISkillLike) { n.Edges.Likes = append(n.Edges.Likes, e) }); err != nil {
			return nil, err
		}
	}
	if query := _q.withSettlements; query != nil {
		if err := _q.loadSettlements(ctx, query, nodes,
			func(n *AISkill) { n.Edges.Settlements = []*AISkillSettlement{} },
			func(n *AISkill, e *AISkillSettlement) { n.Edges.Settlements = append(n.Edges.Settlements, e) }); err != nil {
			return nil, err
		}
	}
	return nodes, nil
}

func (_q *AISkillQuery) loadVersions(ctx context.Context, query *AISkillVersionQuery, nodes []*AISkill, init func(*AISkill), assign func(*AISkill, *AISkillVersion)) error {
	fks := make([]driver.Value, 0, len(nodes))
	nodeids := make(map[int64]*AISkill)
	for i := range nodes {
		fks = append(fks, nodes[i].ID)
		nodeids[nodes[i].ID] = nodes[i]
		if init != nil {
			init(nodes[i])
		}
	}
	if len(query.ctx.Fields) > 0 {
		query.ctx.AppendFieldOnce(aiskillversion.FieldSkillID)
	}
	query.Where(predicate.AISkillVersion(func(s *sql.Selector) {
		s.Where(sql.InValues(s.C(aiskill.VersionsColumn), fks...))
	}))
	neighbors, err := query.All(ctx)
	if err != nil {
		return err
	}
	for _, n := range neighbors {
		fk := n.SkillID
		node, ok := nodeids[fk]
		if !ok {
			return fmt.Errorf(`unexpected referenced foreign-key "skill_id" returned %v for node %v`, fk, n.ID)
		}
		assign(node, n)
	}
	return nil
}
func (_q *AISkillQuery) loadRuns(ctx context.Context, query *AISkillRunQuery, nodes []*AISkill, init func(*AISkill), assign func(*AISkill, *AISkillRun)) error {
	fks := make([]driver.Value, 0, len(nodes))
	nodeids := make(map[int64]*AISkill)
	for i := range nodes {
		fks = append(fks, nodes[i].ID)
		nodeids[nodes[i].ID] = nodes[i]
		if init != nil {
			init(nodes[i])
		}
	}
	if len(query.ctx.Fields) > 0 {
		query.ctx.AppendFieldOnce(aiskillrun.FieldSkillID)
	}
	query.Where(predicate.AISkillRun(func(s *sql.Selector) {
		s.Where(sql.InValues(s.C(aiskill.RunsColumn), fks...))
	}))
	neighbors, err := query.All(ctx)
	if err != nil {
		return err
	}
	for _, n := range neighbors {
		fk := n.SkillID
		node, ok := nodeids[fk]
		if !ok {
			return fmt.Errorf(`unexpected referenced foreign-key "skill_id" returned %v for node %v`, fk, n.ID)
		}
		assign(node, n)
	}
	return nil
}
func (_q *AISkillQuery) loadReviews(ctx context.Context, query *AISkillReviewQuery, nodes []*AISkill, init func(*AISkill), assign func(*AISkill, *AISkillReview)) error {
	fks := make([]driver.Value, 0, len(nodes))
	nodeids := make(map[int64]*AISkill)
	for i := range nodes {
		fks = append(fks, nodes[i].ID)
		nodeids[nodes[i].ID] = nodes[i]
		if init != nil {
			init(nodes[i])
		}
	}
	if len(query.ctx.Fields) > 0 {
		query.ctx.AppendFieldOnce(aiskillreview.FieldSkillID)
	}
	query.Where(predicate.AISkillReview(func(s *sql.Selector) {
		s.Where(sql.InValues(s.C(aiskill.ReviewsColumn), fks...))
	}))
	neighbors, err := query.All(ctx)
	if err != nil {
		return err
	}
	for _, n := range neighbors {
		fk := n.SkillID
		node, ok := nodeids[fk]
		if !ok {
			return fmt.Errorf(`unexpected referenced foreign-key "skill_id" returned %v for node %v`, fk, n.ID)
		}
		assign(node, n)
	}
	return nil
}
func (_q *AISkillQuery) loadLikes(ctx context.Context, query *AISkillLikeQuery, nodes []*AISkill, init func(*AISkill), assign func(*AISkill, *AISkillLike)) error {
	fks := make([]driver.Value, 0, len(nodes))
	nodeids := make(map[int64]*AISkill)
	for i := range nodes {
		fks = append(fks, nodes[i].ID)
		nodeids[nodes[i].ID] = nodes[i]
		if init != nil {
			init(nodes[i])
		}
	}
	if len(query.ctx.Fields) > 0 {
		query.ctx.AppendFieldOnce(aiskilllike.FieldSkillID)
	}
	query.Where(predicate.AISkillLike(func(s *sql.Selector) {
		s.Where(sql.InValues(s.C(aiskill.LikesColumn), fks...))
	}))
	neighbors, err := query.All(ctx)
	if err != nil {
		return err
	}
	for _, n := range neighbors {
		fk := n.SkillID
		node, ok := nodeids[fk]
		if !ok {
			return fmt.Errorf(`unexpected referenced foreign-key "skill_id" returned %v for node %v`, fk, n.ID)
		}
		assign(node, n)
	}
	return nil
}
func (_q *AISkillQuery) loadSettlements(ctx context.Context, query *AISkillSettlementQuery, nodes []*AISkill, init func(*AISkill), assign func(*AISkill, *AISkillSettlement)) error {
	fks := make([]driver.Value, 0, len(nodes))
	nodeids := make(map[int64]*AISkill)
	for i := range nodes {
		fks = append(fks, nodes[i].ID)
		nodeids[nodes[i].ID] = nodes[i]
		if init != nil {
			init(nodes[i])
		}
	}
	if len(query.ctx.Fields) > 0 {
		query.ctx.AppendFieldOnce(aiskillsettlement.FieldSkillID)
	}
	query.Where(predicate.AISkillSettlement(func(s *sql.Selector) {
		s.Where(sql.InValues(s.C(aiskill.SettlementsColumn), fks...))
	}))
	neighbors, err := query.All(ctx)
	if err != nil {
		return err
	}
	for _, n := range neighbors {
		fk := n.SkillID
		node, ok := nodeids[fk]
		if !ok {
			return fmt.Errorf(`unexpected referenced foreign-key "skill_id" returned %v for node %v`, fk, n.ID)
		}
		assign(node, n)
	}
	return nil
}

func (_q *AISkillQuery) sqlCount(ctx context.Context) (int, error) {
	_spec := _q.querySpec()
	if len(_q.modifiers) > 0 {
		_spec.Modifiers = _q.modifiers
	}
	_spec.Node.Columns = _q.ctx.Fields
	if len(_q.ctx.Fields) > 0 {
		_spec.Unique = _q.ctx.Unique != nil && *_q.ctx.Unique
	}
	return sqlgraph.CountNodes(ctx, _q.driver, _spec)
}

func (_q *AISkillQuery) querySpec() *sqlgraph.QuerySpec {
	_spec := sqlgraph.NewQuerySpec(aiskill.Table, aiskill.Columns, sqlgraph.NewFieldSpec(aiskill.FieldID, field.TypeInt64))
	_spec.From = _q.sql
	if unique := _q.ctx.Unique; unique != nil {
		_spec.Unique = *unique
	} else if _q.path != nil {
		_spec.Unique = true
	}
	if fields := _q.ctx.Fields; len(fields) > 0 {
		_spec.Node.Columns = make([]string, 0, len(fields))
		_spec.Node.Columns = append(_spec.Node.Columns, aiskill.FieldID)
		for i := range fields {
			if fields[i] != aiskill.FieldID {
				_spec.Node.Columns = append(_spec.Node.Columns, fields[i])
			}
		}
	}
	if ps := _q.predicates; len(ps) > 0 {
		_spec.Predicate = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	if limit := _q.ctx.Limit; limit != nil {
		_spec.Limit = *limit
	}
	if offset := _q.ctx.Offset; offset != nil {
		_spec.Offset = *offset
	}
	if ps := _q.order; len(ps) > 0 {
		_spec.Order = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	return _spec
}

func (_q *AISkillQuery) sqlQuery(ctx context.Context) *sql.Selector {
	builder := sql.Dialect(_q.driver.Dialect())
	t1 := builder.Table(aiskill.Table)
	columns := _q.ctx.Fields
	if len(columns) == 0 {
		columns = aiskill.Columns
	}
	selector := builder.Select(t1.Columns(columns...)...).From(t1)
	if _q.sql != nil {
		selector = _q.sql
		selector.Select(selector.Columns(columns...)...)
	}
	if _q.ctx.Unique != nil && *_q.ctx.Unique {
		selector.Distinct()
	}
	for _, m := range _q.modifiers {
		m(selector)
	}
	for _, p := range _q.predicates {
		p(selector)
	}
	for _, p := range _q.order {
		p(selector)
	}
	if offset := _q.ctx.Offset; offset != nil {
		// limit is mandatory for offset clause. We start
		// with default value, and override it below if needed.
		selector.Offset(*offset).Limit(math.MaxInt32)
	}
	if limit := _q.ctx.Limit; limit != nil {
		selector.Limit(*limit)
	}
	return selector
}

// ForUpdate locks the selected rows against concurrent updates, and prevent them from being
// updated, deleted or "selected ... for update" by other sessions, until the transaction is
// either committed or rolled-back.
func (_q *AISkillQuery) ForUpdate(opts ...sql.LockOption) *AISkillQuery {
	if _q.driver.Dialect() == dialect.Postgres {
		_q.Unique(false)
	}
	_q.modifiers = append(_q.modifiers, func(s *sql.Selector) {
		s.ForUpdate(opts...)
	})
	return _q
}

// ForShare behaves similarly to ForUpdate, except that it acquires a shared mode lock
// on any rows that are read. Other sessions can read the rows, but cannot modify them
// until your transaction commits.
func (_q *AISkillQuery) ForShare(opts ...sql.LockOption) *AISkillQuery {
	if _q.driver.Dialect() == dialect.Postgres {
		_q.Unique(false)
	}
	_q.modifiers = append(_q.modifiers, func(s *sql.Selector) {
		s.ForShare(opts...)
	})
	return _q
}

// AISkillGroupBy is the group-by builder for AISkill entities.
type AISkillGroupBy struct {
	selector
	build *AISkillQuery
}

// Aggregate adds the given aggregation functions to the group-by query.
func (_g *AISkillGroupBy) Aggregate(fns ...AggregateFunc) *AISkillGroupBy {
	_g.fns = append(_g.fns, fns...)
	return _g
}

// Scan applies the selector query and scans the result into the given value.
func (_g *AISkillGroupBy) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, _g.build.ctx, ent.OpQueryGroupBy)
	if err := _g.build.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*AISkillQuery, *AISkillGroupBy](ctx, _g.build, _g, _g.build.inters, v)
}

func (_g *AISkillGroupBy) sqlScan(ctx context.Context, root *AISkillQuery, v any) error {
	selector := root.sqlQuery(ctx).Select()
	aggregation := make([]string, 0, len(_g.fns))
	for _, fn := range _g.fns {
		aggregation = append(aggregation, fn(selector))
	}
	if len(selector.SelectedColumns()) == 0 {
		columns := make([]string, 0, len(*_g.flds)+len(_g.fns))
		for _, f := range *_g.flds {
			columns = append(columns, selector.C(f))
		}
		columns = append(columns, aggregation...)
		selector.Select(columns...)
	}
	selector.GroupBy(selector.Columns(*_g.flds...)...)
	if err := selector.Err(); err != nil {
		return err
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := _g.build.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}

// AISkillSelect is the builder for selecting fields of AISkill entities.
type AISkillSelect struct {
	*AISkillQuery
	selector
}

// Aggregate adds the given aggregation functions to the selector query.
func (_s *AISkillSelect) Aggregate(fns ...AggregateFunc) *AISkillSelect {
	_s.fns = append(_s.fns, fns...)
	return _s
}

// Scan applies the selector query and scans the result into the given value.
func (_s *AISkillSelect) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, _s.ctx, ent.OpQuerySelect)
	if err := _s.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*AISkillQuery, *AISkillSelect](ctx, _s.AISkillQuery, _s, _s.inters, v)
}

func (_s *AISkillSelect) sqlScan(ctx context.Context, root *AISkillQuery, v any) error {
	selector := root.sqlQuery(ctx)
	aggregation := make([]string, 0, len(_s.fns))
	for _, fn := range _s.fns {
		aggregation = append(aggregation, fn(selector))
	}
	switch n := len(*_s.selector.flds); {
	case n == 0 && len(aggregation) > 0:
		selector.Select(aggregation...)
	case n != 0 && len(aggregation) > 0:
		selector.AppendSelect(aggregation...)
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := _s.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}
