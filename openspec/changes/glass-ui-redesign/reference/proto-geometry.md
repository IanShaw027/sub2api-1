# 原型几何基准（headless Chrome 实测，artboard 内部坐标，左上=0,0；单位 px）

来源：`scratchpad/design/redesign.html` 由 `scratchpad/proto-probe.cjs` 渲染后 getBoundingClientRect。
参考图 PNG（openspec/.../reference/{light,dark}/0N_*.png）里 artboard 内部从 **y=34** 开始（0–31 标签栏，32/33 边框）。

## 像素对比方法（修正版，覆盖旧口径 32）
- 截图高度 = 参考图高 − 35：登录 860、仪表盘/账号 1080、密钥 900、设置 1180。
- `pixel-diff.mjs ... --ref-offset 0,34`，`shift-profile.mjs ... --ref-offset 0,34 --x0 224`。
- 定位偏差用 `UI_SHOTS_PROBE='sel1|sel2' node scripts/ui/shot.cjs ...`（打印元素 top/left/h/w），与本文数字直接比对。

## 壳层（lead 已修，所有后台页共用）
- topbar 0..66（原型 header 高 60 + padding-top 6，content-box，实际 66）；面包屑文字 27..46；右侧按钮组 19..55。
- main 从 66 开始，padding 8 24 24 20 → 内容区 x=244..1416（w1172），y 从 74 开始。
- 紧凑页头：h1 74..109（24px/35px 行高），描述 113..132（13px/19px），页头下间距 14 → 第一块内容 y=146。
- TablePageLayout 高 = 100vh − 98。

## 05 API 密钥（900 高）
- 端点卡行 146..257（h111），三列 1.2fr 1.2fr 1fr = 405 / 405 / 338，gap 12；第三张卡内部 3 等分（95 each）。
- 筛选行 271..307（h36）：搜索 x244 w260；分组 x512 w106；状态分段 x626 w229；右侧「按 创建时间 排序」文本 x1321 w95。
- 表卡 321..876（flex:1），内边 1px：表头 322..365（h43），每行 **59**（365..424..483..542），页脚 824..875（h51，padding 10 16，12.5px）。
- 表格列：`minmax(120px,1fr) minmax(250px,1.4fr) 96 56 100 72 88 80 76 80` → 实测 150,250,96,56,100,72,88,80,76,80（表内容宽 1170，含 16px 左右内边）。
- 行内：名称 600 + `#id` 11.5px mono（gap 2）；key 胶囊 12px mono padding 4 8 radius 7 + 两个 26×26 按钮；分组胶囊 11.5px padding 3 8 radius 6；并发 12.5px mono。

## 04 账号管理（1080 高）
- 统计行 146..199（h53），5 等分 226。
- 筛选行 213..249（h36）：搜索 x244 w260；平台 x512 w106；分组 x626 w106；类型 x740 w179；可调度 x927 w132；「已选 0 / 86」x1308；末尾 36×36 按钮 x1380。
- 表卡 263..1056：表头 264..307（h43），每行 **61**（307..368..429..490..551），页脚 1004..1055（h51）。
- 表格列：`32 minmax(170px,1.6fr) 100 80 64 84 60 120 1.2fr 52 88 64` → 实测 32,170,100,80,64,84,60,120,92,52,88,64。
- 复选框 16×16 radius 5 border 1.5；类型胶囊 11.5px padding 3 7 radius 6。

## 03 管理员仪表盘（1080 高，main overflow hidden，不滚动）
- Hero 74..293（h219，padding 26 30，grid 1.25fr 1fr = 603/483，gap 24）；左列文案 101..266；h1 131..166；p 176..217；右侧三小卡 128..239（h111，3×154）。
- StatCard 网格 309..558：两行，卡 309..429（h120）/ 441..558（h117），4 列 284，gap 12。
- 图表行 574..817（h243）：请求趋势 x244 w773 / 模型分布 x1029 w387（2fr 1fr）。
- 底部 833..1056（h223）：平台健康 x244 w580 / 最近事件 x836 w580；健康行 grid 120px 1fr 150px。
- 段间距 16（14 在 04/05）。

## 06 系统设置（1180 高）
- 146..1156：左导航 224 + 内容 1fr（x482 w934），gap 14；左导航列表 146..486，部署信息卡 498..641。
- 内容区：站点设置卡 146..753；自定义端点 765..898；通用表格设置 910..1116。
- 表单行 grid 240px 1fr，行高 67（单行）/ 84（带说明）。
