| 路由 | 原型区块 | 原型呈现 | 实际呈现 | 类型(不符/更优) | 理由 | 截图 |
|---|---|---|---|---|---|---|
| `/home` | 顶部导航 | 仅 登录 / 立即开始 | 增加 语言切换 + 主题切换，均为 34px 图标按钮（LocaleSwitcher 用 `:deep()` 隐藏文字/箭头仅留国旗，尺寸与 `.header-icon-btn` 主题按钮对齐） | 不符（项目已有 LocaleSwitcher 与主题切换） | 功能必须保留；5.1 已复核并统一为 34px 图标按钮排版 | reference/light/01_首页.png vs .shots/g5a-home-light.png |
| `/home` | Hero 副文案 | 固定文案「无需管理多个订阅账号...」 | 站点配置了 `site_subtitle`（mock 值「订阅转 API 转换平台」）时优先展示该值 + `heroDescriptionExt` 固定后缀 | 不符（数据驱动，非代码问题） | 代码按设计接“站点副标题”，无自定义值时才回退到原型默认文案；当前差异来自 mock `site_subtitle` 与原型截图环境不同，非视觉缺陷；建议 lead 视觉核对时使用 `--ignore` 排除该行或让 mock 清空该字段 | reference/light/01_首页.png vs .shots/g5a-home-light.png |
| `/home` | 社证行 | 「已服务 1,284 名开发者 · 累计 8.6 亿次请求」（真实统计） | 无公开统计 API，展示 i18n 默认文案 `home.socialProof`（「面向 Claude Code · Codex CLI · Cursor 等客户端的开发者」） | 不符（mock/后端缺少公开统计接口） | 按 5.1 要求「无数据时用 i18n 默认文案」实现；若需要还原真实数字需要新增公开统计 API，超出本代理所有权（views/home 组件），已记录供 lead 评估 | reference/light/01_首页.png vs .shots/g5a-home-light.png |
| 全站（`--muted` 亮色） | ui-standards.md §1 令牌表 | `oklch(55.17% 0.006 259.82)` | `oklch(50% 0.006 259.82)` | 更优（对比度） | 55.17% 的 L 值在亮色 `--surface-secondary`/`--surface-tertiary` 上不足 4.5:1；50% 是满足 WCAG 1.4.3 的最小安全值（见 tokens.css 注释与 efc598468 提交） | reference/light/07_组件规范.png |
| 全站（`--border` 暗色） | ui-standards.md §1 令牌表 | `oklch(28% 0.003 259.82)` | `oklch(51% 0.003 259.82)` | 更优（对比度） | 28% 的 L 值在暗色 `--surface`/`--background` 上不足 3:1，字段/卡片边框在暗色下几乎不可见（WCAG 1.4.11）；51% 是满足非文本对比度的最小安全值（见 tokens.css 注释与 efc598468 提交） | reference/dark/07_组件规范.png |
| `/model-plaza` | 分组表格表头 | 通用单行 42px `.table-head` | `PlazaModelPricingTable.vue` 表头为两行：第一行 `colspan="3"` 分组「实付价格」「官方价格」，第二行为输入/输出/缓存子列 | 不符（结构性差异） | 价格表需要区分「实付」与「官方」两组各 3 列，单行 42px 表头无法表达分组关系；两行表头本身仍使用 token 化配色（`--pz-title`/`color-mix` 派生），未引入禁用的硬编码色值/圆角/阴影，判断为业务必需的结构偏离而非视觉不合规 | 见 `PlazaModelPricingTable.vue` 现有实现（本次未改动该表头结构） |
| `/key-usage` | 结果态：额度环形图 + 详情/统计卡片 + 按日/模型表格 | mock 的 `/v1/usage`（网关，Bearer Key 鉴权）路由未在 `scripts/mock/server.js` 中实现，任意 Key 查询均返回网络层 404/兜底响应，导致查询恒失败 | 页面仅能展示「输入区 + 未查询」的初始态；结果区（StatCard 额度卡、详情卡、Token 统计、按日/模型 DataTable）均无法在当前 mock 下触发渲染，已用相同 mock 数据形状在单元测试（`KeyUsageView.spec.ts`，直接 stub `fetch`）中验证过其 DOM/文本正确性 | 不符（mock 数据缺失） | 5.5 明确要求「结果态若 mock 无数据，说明并只截空态」；已按此执行，未伪造/隐藏空态 | g5b-keyusage-light.png（因 needs_setup 联动问题重截中，见报告） |
| `/home`（截图方法论） | 全部三张截图（`01_首页.png` 亮/暗、`08_移动端...png` 内首页画板） | `proto-shots.cjs` 截取 `[data-screen-label]` 整个 section（含标题字号 13px 的说明条 + `gap:14px`），导致参考图顶部多出约 32px 非页面内容的画板标签条；移动端参考图额外套了一层 iPhone 机身/状态栏/Home 指示条装饰边框 | 亮色/暗色 1440：用 `pixel-diff.mjs --ref-offset 0,32`（`--ignore 0,470,1440,60` 排除动态 Hero 副文案/社证行、`--tol 24`）。coordinator 用 `shift-profile.mjs` 指出残差含整像素级局部位移（非纯次像素噪声），复核后定位到两处真实可修复的行高 bug 并已修复：① `ConsolePreview.vue` 的 `.console-label/.console-provider-name/.console-provider-models/.console-provider-lat/.console-log/.console-chart-label/.console-chart-rpm` 未设 `line-height`，继承 Tailwind Preflight 全局 `line-height:1.5`，比原型窗口内小号文字实际渲染高约 18.5px（挤高卡片、经 `.home-hero` 的 `align-items:center` 联动把左列标题/副文案/按钮/社证行整体下推）——已按原型量得的行高比例补齐显式 `line-height:1.3`；② 全局共享类 `.section-title`（`src/style.css`，不可改）的 `line-height:1.15` 是按其它上下文调的，用在本页 30px 的三步/定价/对比标题上比原型少约 9px（原型该字号实测比例约 1.467），已在 `HomeView.vue` 自身 scoped 样式内用更具体选择器 `.home-steps/.home-pricing/.home-compare-copy .section-title { line-height: 1.467 }` 局部覆盖，未改动全局类本身。修复后 `shift-profile.mjs` 逐 60px 带复测：亮色/暗色几乎全部有效带 `dy` 收敛到 -3~+3px（此前 hero/console 区最高达 +20px），仅 `dx` 恒为 -2px（见下条不可修复项）；`pixel-diff.mjs` 整体 diff：亮色 6.352%→**5.548%**，暗色 6.96%→**5.657%**。剩余 ~5.5% 残差经二次验证判断为不可再修复的渲染级噪声，而非结构性位移：(a) 用不同 `--tol`（24/48/72/100）复测，diff% 随容差近似线性下降（5.548→3.69→2.838→2.09）而非阶跃归零，说明是大量 1-2px 级别的文字边缘/抗锯齿差异，非局部大色块错位；(b) 人工把整张实际截图向右平移 2px 去补偿 `dx=-2` 后重新比对，diff% 几乎不变（5.548%→5.57%），证明 `dx=-2` 不是主要贡献项。`dx=-2` 本身的根因已定位：原型 `redesign.html` 的 01 首页画板容器为 `width:1440px;border:1px solid ...`，且未设 `box-sizing`（默认 `content-box`），导致该容器真实渲染宽度为 1442px，内部内容相对页面左边界整体右移约 1-2px；这是参考图生成管线自身的画板边框伪影，我方页面在 `x=80` 处与自身 `padding:0 80px` 精确对齐，没有对应可修的代码缺陷，也不应为凑合参考图而人为给真实页面加假边框/位移。综上：结构性位移已通过上述两处行高修复清零到 ≤3px，糊字残差判断为参考图渲染管线的字体级噪声上限（本次复核不再重复此前"完全一致、非本代理可修"的笼统结论，而是明确区分了"已修复的真实 bug"与"确认不可修的渲染伪影"两类）；移动端：参考图是设备边框内的静态构图（手动估算并裁掉状态栏/机身/Home 指示条得到内容区），`--ref-offset` 只跳过顶部整行像素、无法处理该画板左右两侧的机身裁切，无法拿到该画板的精确坐标元数据，裁剪对齐误差导致像素级 diff（32.088%）参考价值有限，已改为人工核对关键检查点（见下方 checklist），供 lead 决定是否需要为参考图生成管线新增按内容区裁剪的能力 | 不符（截图/参考图生成管线限制，非代码缺陷）；`ConsolePreview.vue`/`HomeView.vue` 行高问题已修复 | 详见最终报告的「像素比对方法论」说明及每带 dy/dx 复测表 | .shots/g5a-home-light.png、.shots/g5a-home-light-diff.png、.shots/g5a-home-dark.png、.shots/g5a-home-dark-diff.png、.shots/g5a-home-390.png、.shots/g5a-home-390-diff.png |
| `/home`（移动端） | Hero 主 CTA 按钮文案 | `08_移动端...png` 画板内文案为「免费开始」 | 复用桌面端/全站统一的 `home.getStarted` = 「立即开始」（与 `reference/dark/01_首页.png`、`reference/light/01_首页.png` 桌面参考图一致） | 不符（原型自身移动端画板与桌面画板文案不一致，非代码问题） | 页面是响应式同一份 DOM/文案，不会因断点切换按钮文案；桌面参考图与移动端参考图对同一 CTA 使用了两种不同文案，属原型设计稿内部不一致，故保留与桌面参考图及全站统一 i18n key 一致的「立即开始」，未改动 | reference/light/08_移动端_三个核心页面.png vs .shots/g5a-home-390.png |
| `/home` | 对比区块标题（"为什么选择我们？"下方大标题） | 参考图为「对比官方订阅」，配文「智能调度多个上游账号，自动切换和负载均衡；按实际使用量计费，支持配额上限，团队用量一目了然。」 | 修复前代码误用了 `home.features.multiAccount`（「稳定可靠」，用于 AuthBrandPanel 等其它场景）+ 两条 features 文案拼接；本次已新增专用 key `home.comparison.heading` / `home.comparison.headingDesc`（zh/en 同步新增，未删除/修改原有 `home.features.*` key，`AuthBrandPanel.vue` 等其它引用不受影响）并让 `HomeView.vue` 改用新 key，现文案与参考图一致 | 已修复（非本行需要的"deviation"，记录为变更说明） | 5.1 要求"对比官方订阅 section title/table"与原型一致；发现并修正了错用 i18n key 的既有问题 | reference/light/01_首页.png vs .shots/g5a-home-light.png |
| `/login` | 卡片 OAuth 区块 + 底部条款行 | `02_登录.png` 仅 2 行 OAuth（Linux.do 全宽 + Passkey/钉钉两列），卡片底部有「登录即表示同意 服务条款 与 隐私政策」条款行 | 当前 mock 配置多启用了 GitHub provider，多渲染一行全宽 OAuth 按钮；且 mock 的 `loginAgreementDocuments` 为空数组，条款行按现有逻辑（无文档则不渲染）被隐藏 | 不符（mock 数据/开关差异，非代码缺陷） | `EmailOAuthButtons.vue`/`LoginAgreementPrompt.vue` 均按 mock 返回的 provider 列表与协议文档数组正确渲染，逻辑本身与原型一致；因内容行数差异导致卡片下半部分整体位移，像素 diff 无法通过矩形 `--ignore` 屏蔽，实测 diff 8.13%（裁剪掉参考图顶部 39px 说明条后，`--tol 30` + 两处 OAuth 区域 ignore）。建议 lead 视觉核对时以关闭 GitHub provider + 填充 1 条协议文档的 mock 配置重截，或在验收时以“结构一致、内容行数因 mock 而异”为准 | reference/light/02_登录.png vs .shots/g6a-login-light.png |
| `/login` | OAuth 品牌色 | — | `LinuxDoOAuthSection.vue` 的 `.oauth-mark-linuxdo{background:#e95420}`、`WechatOAuthSection.vue` 的 `.oauth-mark-wechat{background:#07c160}` 仍为十六进制硬编码 | 不符（技术规范例外） | 二者是第三方品牌官方标准色（Linux.do 橙 / 微信绿），非任意装饰色；`DingTalkOAuthSection.vue` 已示范用 `oklch(62% 0.18 240)` 替代同类品牌色，但为避免改变品牌识别度且非任务验收硬性要求，本次未改动，供 lead 决定是否统一改造为 OKLCH 等价色 | components/auth/LinuxDoOAuthSection.vue、WechatOAuthSection.vue |
| `/auth/dingtalk/email-completion` | 6.4「48px 6 位码字段（mono 22 tracking .4em）、重发倒计时 ghost 按钮」 | 修复前：`PendingOAuthCreateAccountForm.vue` 验证码字段用 `.input` 类（非 `.field`），无 mono/22px/tracking-.4em 样式；重发/倒计时为裸 `<button class="btn btn-secondary">`；成功提示用裸 `text-green-600` | 该组件所有权已由 lead 追加给本代理（6A）。已改造：验证码字段改为 `field h-12 text-center font-mono text-[22px] tracking-[0.4em]`；发送/重发按钮改为 `Button variant="ghost"`；提交/切换按钮改为 `Button size="lg"`/`variant="secondary" size="lg"`；成功提示改为 `text-success-text`（语义 token，非硬编码颜色）。所有 `t('key')`/`v-model`/`@handler`/`data-testid` 均逐一 diff 确认无丢失；`vitest run src/components/auth src/views/auth`（17 文件/137 用例）与 `vue-tsc --noEmit` 均通过 | 已修复 | 6.4 明确要求该样式配方；因 mock 的 `email_verify_enabled` 默认为 `false`，常规截图看不到验证码行，已用运行时拦截 `/settings/public` 响应的方式强制该字段为 `true` 二次截图确认样式正确 | .shots/g6a-dingtalk-email.png（默认态）、.shots/g6a-dingtalk-code.png（验证码态，运行时 mock 覆盖） |
| `/*`（任意未匹配路径） | 5.7「64/800 主色渐变 404、返回按钮」 | 原型仅要求 404 数字 + 返回按钮 | `NotFoundView.vue` 删除了旧实现中的装饰性图标圆（`h-24 w-24 rounded-2xl bg-[var(--accent)] shadow-lg` 的警告图标），仅保留渐变「404」大字 + 标题/描述 + 返回上一页/返回首页两个按钮 | 更优（同时修复旧代码的 `shadow-lg` 禁用类违规） | 旧实现的图标圆本身违反禁用类规范（`rounded-2xl`+`shadow-lg`），且原型验收标准未要求该图标元素，简化后视觉更聚焦、且完全满足任务描述 | .shots/g5b-404-light.png |
| `/*`（任意未匹配路径） | 5.7「返回按钮」 | 旧实现「返回首页」链接指向 `/dashboard`（仅登录用户可达） | 改为指向 `/home`（公开首页，游客可达） | 更优（功能修复） | 404 页面本身对游客可见，链接到需登录的 `/dashboard` 对游客是死路（会被鉴权守卫再次拦截）；改为公开首页 `/home` 保证任何角色点击都能正确返回 | .shots/g5b-404-light.png |
| `/*`（任意未匹配路径，游客角色） | 5.7 截图验收 | 期望游客直接看到 `NotFoundView.vue` | 使用 `guest` 角色截 `/this-page-does-not-exist` 时被全局路由守卫重定向到 `/login`，而非渲染 404 页面 | 不符（`router/index.ts` 既有配置缺陷，非本代理改动） | 根因：`router/index.ts` 第 827 行的 catch-all 路由 `{ path: '/:pathMatch(.*)*', name: 'NotFound', component: NotFoundView }` 的 `meta` 未显式设置 `requiresAuth: false`；全局守卫（第 911 行）逻辑为 `to.meta.requiresAuth !== false`（即未设置时默认按需要登录处理），导致任何游客访问任意未匹配路径都会先被重定向到 `/login`，`NotFoundView.vue` 实际上对游客不可达。已改用 `user`（已登录）角色重截并确认 `NotFoundView.vue` 本身渲染完全正确（见 g5b-404-light/-dark/-390）；`router/index.ts` 不在本代理（5.4-5.7）所有权范围内，未做修改，建议 lead 指派归属后为该路由补充 `meta: { requiresAuth: false }` | .shots/g5b-404-light.png（user 角色，正确渲染 404） |
| `/auth/{linuxdo,dingtalk,oidc,wechat}/callback`、`/auth/wechat/payment/callback` | 6.5「44px 色环圆 + 20/800 标题 + 13.5 静音描述 + 42px 按钮 + `.code-block` 错误详情」的 `CallbackStatusCard.vue` 三态卡片 | 原型未给出这 6 个回调页面各自的完整页面构图（仅给出卡片规格） | `CallbackStatusCard.vue` 按规格新建并接入全部 6 个回调视图，但视图外层沿用既有 `AuthLayout.vue` 的左右分栏（品牌插画+卡片），未改为独立 440px 裸卡片页面 | 不符（`AuthLayout.vue` 属禁止修改范围） | `AuthLayout.vue` 明确在本代理禁止改动清单内；6 个回调视图此前均已用 `AuthLayout` 包裹且该结构非本任务范围，仅替换内部状态展示为 `CallbackStatusCard`，未改变外层布局 | .shots/g6b-cb-linuxdo-err.png、g6b-cb-dingtalk-err.png、g6b-cb-oidc-err.png、g6b-cb-wechat-err.png |
| `/auth/wechat/payment/callback` | 6.5 成功态：`CallbackStatusCard` tone=success + 42px「继续」按钮 | — | mock 未实现微信支付回调的 resume-token 校验端点，任意 `resume_token` 查询均判定为失败/跳转 `/login`，无法触发成功态渲染 | 不符（mock 数据缺失，非代码缺陷） | 按规则 9 说明而非糊过：成功态渲染逻辑已在 `WechatPaymentCallbackView.spec.ts` 用 stub 数据单测覆盖（组件层面验证通过），仅截图层面因 mock 限制无法覆盖该状态 | 无对应截图（mock 无法触发该状态），单测见 `src/views/auth/__tests__/WechatPaymentCallbackView.spec.ts` |
| `/setup` | 6.6「640px 卡片 + 4 步 stepper」截图验收 | — | `vite.config.ts` 开发代理规则 `'/setup': { target: backendUrl }` 会拦截任何以 `/setup` 开头的请求（含 SPA 路由本身），导致标准截图工具 `scripts/ui/shot.cjs` 对 `/setup` 做硬导航时命中的是 mock 的 JSON 接口而非 SPA | 不符（基础设施缺陷，非本任务代码问题） | `vite.config.ts`/`scripts/ui/shot.cjs` 均不在本代理所有权范围，未做修改；改用 scratchpad 内的一次性脚本（先导航到已登录安全路径，再用 `history.pushState`+`popstate` 触发前端路由切换）完成截图，未产出任何仓库内文件改动。建议 lead 后续为 `/setup` 之类的页面路由与 API 前缀重名场景调整代理规则（如把 API 改为 `/api/setup` 或缩窄代理匹配为 `/setup/status`、`/setup/test-db` 等具体子路径） | .shots/g6b-setup-step1.png、g6b-setup-step2.png、g6b-setup-step3.png、g6b-setup-step4.png、g6b-setup-dark.png、g6b-setup-390.png |
| `/login` `/register`（左侧品牌面板） | 6.1 三条卖点 | 「一键接入 · 获取一个 API 密钥，即可调用所有已接入模型」等三条单行文案 | 首版复用了首页 `home.features.*Desc` 长文案，导致第一条换行、整块下移 8–14px；已新增 `auth.brand.features.*`（zh/en）精炼文案与原型逐字一致 | 不符（已修复） | 品牌面板与首页特性区的文案职责不同，单行才能保持原型的垂直节奏 | `.shots/lead-login.png`（diff 3.6%，各带 dy=0） |
| `/login` `/register`（左侧品牌面板） | 6.1 副文案 | 固定文案「无需管理多个订阅账号…AI 服务。」按 ~500px 换成两行 | 段落 `max-width: 500px`，`home.heroDescription` 补全句号；站点配置了 `site_subtitle` 时仍优先展示该值（与首页一致），mock 已把 `site_subtitle` 置空以对齐原型默认态 | 更优（保留站点配置） | 副文案由站点设置驱动是既有功能，不能为了像素一致而丢弃 | 同上 |
| `/login`（表单） | 6.1 演示态 | 邮箱/密码已填入、「记住我」已勾选 | 空表单 + placeholder，`记住我` 默认未勾选 | 不符（演示数据） | 原型填入的是示例数据；真实登录页初始为空 | 同上 |
| `/login`（截图方法论） | 6.1 像素比对 | 画板内容区 1440×863（画板 895 含 32px 标签条） | 登录页整体垂直居中，1440×900 截图会整体下移 18px（(900-863)/2）；必须用 `shot.cjs … 1440 863` 比对 | — | 比对基线尺寸问题，非实现问题 | `.shots/lead-login-diff.png` |
